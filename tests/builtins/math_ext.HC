U0 Main() {
    "%d\n", ACos(1.0) == 0.0 ? 1 : 0;
    "%d\n", ASin(0.0) == 0.0 ? 1 : 0;
    "%d\n", Sinh(0.0) == 0.0 ? 1 : 0;
    "%d\n", Cosh(0.0) == 1.0 ? 1 : 0;
    "%d\n", Tanh(0.0) == 0.0 ? 1 : 0;
    "%.1f\n", FMod(5.0, 3.0);
    "%.4f\n", Cbrt(8.0);
    "%.1f\n", Trunc(3.7);
}
